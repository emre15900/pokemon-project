import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0072B1",
    },
    secondary: {
      main: "#E68306",
    },
    error: {
      main: "#ff1744",
    },
  },
});

export default theme;
